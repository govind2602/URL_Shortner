<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - URL Shortener</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #f6f8fa;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: auto;
            margin: 40px auto;
            background: #fff;
            border-radius: 12px;
            box-shadow: 0 4px 24px rgba(154, 23, 23, 0.08);
            padding: 2rem 2.5rem 2.5rem 2.5rem;
        }

        h1 {
            text-align: center;
            color: #6366f1;
            margin-bottom: 2rem;
        }

        .generated-url {
            background: #e0e7ff;
            color: #3730a3;
            padding: 1rem;
            border-radius: 8px;
            margin-bottom: 1.5rem;
            text-align: center;
            font-size: 1.1rem;
            word-break: break-all;
        }

        form {
            display: flex;
            flex-direction: column;
            gap: 1rem;
            margin-bottom: 2rem;
        }

        label {
            font-weight: 500;
            color: #555;
        }

        input[type="text"] {
            padding: 0.7rem;
            border: 1px solid #d1d5db;
            border-radius: 6px;
            font-size: 1rem;
            background: #f9fafb;
            transition: border 0.2s;
        }

        input[type="text"]:focus {
            border-color: #6366f1;
            outline: none;
        }

        button {
            padding: 0.8rem;
            background: #6366f1;
            color: #fff;
            border: none;
            border-radius: 6px;
            font-size: 1.1rem;
            font-weight: bold;
            cursor: pointer;
            transition: background 0.2s;
        }

        button:hover {
            background: #4f46e5;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 1.5rem;
            background: #f9fafb;
            border-radius: 8px;
            overflow: hidden;
        }

        th,
        td {
            padding: 0.8rem;
            text-align: center;
        }

        th {
            background: #6366f1;
            color: #fff;
        }

        tr:nth-child(even) {
            background: #e0e7ff;
        }

        tr:nth-child(odd) {
            background: #fff;
        }

        .short-link {
            color: #3730a3;
            text-decoration: none;
            font-weight: 500;
        }

        .short-link:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>URL Shortener</h1>
        <% if(locals.id){ %>
            <div class="generated-url">
                URL Generated: <a class="short-link" href="http://localhost:8001/url/<%= id %>" target="_blank">
                    http://localhost:8001/url/<%= id %>
                </a>
            </div>
            <% } %>
                <form action="/url" method="post">
                    <label for="url">Enter your Original URL</label>
                    <input type="text" id="url" name="url" placeholder="https://example.com" required>
                    <button type="submit">Generate</button>
                </form>
                <% if(locals.urls && urls.length> 0){ %>
                    <table>
                        <thead>
                            <tr>
                                <th>S. No</th>
                                <th>Short URL</th>
                                <th>Original URL</th>
                                <th>Clicks</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% urls.forEach((url, index)=> { %>
                                <tr>
                                    <td>
                                        <%= index + 1 %>
                                    </td>
                                    <td>
                                        <a class="short-link" href="http://localhost:8001/<%= url.shortId %>"
                                            target="_blank">
                                            http://localhost:8001/<%= url.shortId %>
                                        </a>
                                        <button type="button" style="
        margin-left: 8px;
        padding: 4px 8px;
        font-size: 0.8rem;
        background: #4f46e5;
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
    " onclick="navigator.clipboard.writeText('http://localhost:8001/<%= url.shortId %>').then(()=>alert('Copied to clipboard!'))">
                                            📋 Copy
                                        </button>
                                    </td>

                                    <td>
                                        <a class="short-link" href="http://localhost:8001/url/<%= url.shortId %>"
                                            target="_blank">
                                            http://localhost:8001/<%= url.shortId %>
                                        </a>
                                    </td>
                                    <td style="word-break: break-all;">
                                        <%= url.redirectURL %>
                                    </td>
                                    <td>
                                        <%= url.visitHistory.length %>
                                    </td>
                                </tr>
                                <% }); %>
                        </tbody>
                    </table>
                    <% } %>
    </div>
</body>